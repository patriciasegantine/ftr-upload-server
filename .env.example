# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# NODE_ENV: 'development', 'test', or 'production'
NODE_ENV="development"
PORT="3333"

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# These variables are used by Docker Compose to initialize the DB container.
POSTGRES_USER="docker"
POSTGRES_PASSWORD="docker"
POSTGRES_DB="upload"
POSTGRES_PORT="5432"

# Connection String
# This is used by the Node.js app when running locally (npm run dev).
# IMPORTANT: When running via Docker Compose, this value is automatically
# overridden internally to connect to the 'db' service instead of 'localhost'.
DATABASE_URL="postgresql://docker:docker@localhost:5432"

# -----------------------------------------------------------------------------
# Storage Configuration (Cloudflare R2 / S3 Compatible)
# -----------------------------------------------------------------------------
CLOUDFLARE_ACCOUNT_ID="your_account_id"
CLOUDFLARE_ACCESS_KEY_ID="your_access_key_id"
CLOUDFLARE_SECRET_ACCESS_KEY="your_secret_access_key"
CLOUDFLARE_BUCKET="your_bucket_name"
CLOUDFLARE_PUBLIC_URL="https://pub-Txwq.r2.dev"

# Notes:
# 1. Copy this file to `.env`: cp .env.example .env
# 2. Never commit the real `.env` file to Git.
# 3. If a value contains spaces, wrap it in double quotes.