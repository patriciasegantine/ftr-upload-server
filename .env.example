# App settings
# - `NODE_ENV` accepted values: `development`, `test`, `production`.
#   Use `development` locally, `test` for CI/tests, and `production` when deployed.
PORT=3333
NODE_ENV=development

# Database
# Format: postgresql://user:pass@host:port/database
DATABASE_URL=postgresql://docker:docker@localhost:5432/upload_test

# Cloudflare R2 / bucket credentials
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_ACCESS_KEY_ID=your_key_id
CLOUDFLARE_SECRET_ACCESS_KEY=your_secret
CLOUDFLARE_BUCKET=your_bucket
CLOUDFLARE_PUBLIC_URL=http://localhost

# Notes:
# - Copy this file to `.env` and fill the values before running the app.
# - Do NOT commit `.env` to source control. Add `.env` to your `.gitignore`.
# - If a value contains spaces, wrap it in double quotes.
